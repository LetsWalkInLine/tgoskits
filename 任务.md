# StarryOS Futex 与 Robust-List 语义完善：主 Agent 启动提示词

## 你的角色

你是本轮工作的主 agent 和技术监工。你的职责是把控范围、调度 subagent、整合结果、验证质量，并确保实现既完成当前语义目标，又不堵死后续 PI futex、`FUTEX_WAKE_OP`、ptrace 权限、execve 生命周期等扩展路径。

本轮应采用“语义契约驱动”的协作方式：测试 agent 和实现 agent 可以同时启动，各自只依据 Linux 语义和本文档工作。测试 agent 不需要知道 StarryOS 当前实现，也不应为了当前实现定制测试；实现 agent 不应修改测试预期来迁就实现。

核心原则：在本轮范围内尽量贴近 Linux 语义完成可验证实现，同时保持代码结构和接口对后续扩展兼容。范围边界是工作要求，不是主要目标；主要目标是语义正确、测试充分、演进空间清晰。

## 目标

以 Linux `futex(2)`、`set_robust_list(2)`、`get_robust_list(2)` 语义为基准，完善 StarryOS 中本轮范围内的 futex 基础能力和普通 robust-list owner-death 路径。

本轮重点：

- 补强用户态测试，覆盖本轮所有 in-scope 语义。
- 修复本轮范围内 futex 的 op/flag 解析、private/shared key、参数校验、timeout、WAIT/WAKE/BITSET/REQUEUE/CMP_REQUEUE 语义。
- 补齐 robust-list 普通 futex owner-death 的关键边界测试和实现。
- 对 PI futex、`FUTEX_WAKE_OP`、ptrace 权限、execve robust cleanup 等后续扩展项保留清晰边界和兼容空间。

## 硬性约束

- 不在宿主机安装工具链或依赖。
- 所有 build/test/lint/fmt 命令必须在 Docker 容器内运行。
- 优先使用 `docker compose run --rm tgoskits <command>`。
- StarryOS 相关验证使用 `cargo xtask ...`，不要直接裸跑 `cargo build/test/run`。
- 修改逻辑后至少运行：
  - `docker compose run --rm tgoskits cargo fmt`
  - `docker compose run --rm tgoskits cargo xtask clippy --package starry-kernel`
- 不用 `#[allow(...)]` 静音 clippy，除非用户明确要求。
- `scripts/test/clippy_crates.csv` 当前已包含 `starry-kernel`，本轮通常不需要修改。
- 开发环境性能有限，命令会跑很长时间，请保持充分耐心

## 当前事实基线

### 相关文件

- syscall 层：`os/StarryOS/kernel/src/syscall/sync/futex.rs`
- futex 等待队列、key、table：`os/StarryOS/kernel/src/task/futex.rs`
- robust-list 退出扫描：`os/StarryOS/kernel/src/task/ops.rs`
- per-thread robust list head：`os/StarryOS/kernel/src/task/mod.rs`
- 现有主测试：`test-suit/starryos/normal/qemu-smp1/syscall/test-futex-robust-list/c/src/main.c`
- futex 回归测试：
  - `test-suit/starryos/normal/qemu-smp1/bugfix/bug-futex-wait-wake`
  - `test-suit/starryos/normal/qemu-smp4/test-futex-race`

### 当前已实现能力

| 能力 | 当前状态 |
|---|---|
| `FUTEX_WAIT` | 已有基础实现，支持值检查、等待、timeout、中断 |
| `FUTEX_WAKE` | 已有基础实现，按 count 唤醒 |
| `FUTEX_WAIT_BITSET` / `FUTEX_WAKE_BITSET` | 已有基础实现，已校验 `val3 != 0` |
| `FUTEX_REQUEUE` / `FUTEX_CMP_REQUEUE` | 已有基础实现，但参数校验和序列化语义需加强 |
| `set_robust_list` | 已实现 size 校验和当前线程 head 保存 |
| `get_robust_list` | 已实现 tid 查询和 head/size 写回；未做 ptrace 权限 |
| robust owner death | `do_exit()` 调用 `exit_robust_list()`，设置 `FUTEX_OWNER_DIED` 并唤醒 waiter |
| shared futex | `FutexKey` 当前按 VMA backend 自动区分 shared/private |

### 本轮要修的差距

| 差距 | 处理策略 |
|---|---|
| `FUTEX_PRIVATE_FLAG` 未显式控制 futex key | 本轮修复 |
| `FUTEX_CLOCK_REALTIME` 和未知 option 校验不完整 | 本轮修复 |
| `FUTEX_WAKE`/`REQUEUE` 对 `uaddr`/`uaddr2` 校验不足 | 本轮修复 |
| `FUTEX_WAKE` count 负值语义不完整 | 本轮修复 |
| `FUTEX_CMP_REQUEUE` compare 与 wake/requeue 序列化不足 | 本轮尽量修复到当前范围内的 Linux 语义 |
| robust-list `list_op_pending`、owner mismatch、坏链/limit 等边界覆盖不足 | 本轮补测试并修复普通 futex owner-death 路径 |

### 后续扩展项

| 项目 | 暂缓原因 |
|---|---|
| PI futex 全套 | 需要 scheduler priority、PI lock、owner transfer，本轮只保留扩展空间 |
| `FUTEX_WAKE_OP` | 涉及用户态原子 read-modify-write 和双 futex wake，后续单独 milestone |
| `get_robust_list` ptrace 权限 | 涉及 credential/ptrace 权限模型，可能跨模块 |
| `execve` robust-list cleanup | 涉及 task/exec 生命周期，后续单独处理 |
| 跨架构压力和性能调优 | 先保证语义正确和回归稳定 |

完成本轮工作后，列出相对于完整的linux语义还需要做哪些工作。

## 主 Agent 调度方式

你应并行启动 test agent 和 implementation agent。二者共享本文档中的 Linux 语义目标和范围，但写入范围必须隔离：

- Test agent 只写用户态测试。
- Implementation agent 只写内核实现。
- Review agent 在两者完成后只读审查 diff。

主 agent 负责整合时必须保持监工视角：

- 不允许 implementation agent 改测试预期来通过测试。
- 不允许 test agent 根据当前实现“反向设计”测试。
- 涉及 PI、ptrace、execve robust cleanup 等后续扩展项时，只记录接口影响和扩展建议，不在本轮展开实现。
- 若测试和实现出现分歧，先按 Linux man page 判定语义，再决定修测试还是修实现。
- 如果发现本文档中的某条测试预期与 Linux 明确不符，必须记录证据并修正文档/测试，而不是静默调整。

## Subagent 1：Test Agent 任务

### 角色与范围

你是独立测试设计 agent。你不知道 StarryOS 的具体实现，也不需要研究内核代码。你只依据 Linux 语义编写用户态测试，用效果定义正确性。

写入范围：

- 默认只修改：`test-suit/starryos/normal/qemu-smp1/syscall/test-futex-robust-list/c/src/main.c`
- 除非主 agent 明确批准，不新增测试 binary，不修改 qemu toml，不修改内核文件。

### 测试设计原则

- 严格按 Linux futex/robust-list 语义写测试，不迁就 StarryOS 当前行为。
- 新增测试不要求在当前实现上必然失败；可能通过、可能失败。你的目标是定义正确行为。
- 所有阻塞 wait 必须带 timeout，避免永久挂死。
- 并发和跨进程测试必须使用可靠同步：shared atomic、pipe、ready flag、`waitpid`、`pthread_join`。
- 不依赖过短 sleep 判断语义；sleep 只能作为 settle 辅助。
- 测试输出要明确写出 Linux ABI 预期，便于 implementation agent 和 review agent 定位。

### 详细任务清单

1. op/flag 校验测试
   - 未知 futex op 返回 `ENOSYS`。
   - 未知 option bits 返回 `EINVAL`。
   - `FUTEX_CLOCK_REALTIME` 只在 Linux 允许的 wait 类操作上接受。
   - `FUTEX_WAKE`、`FUTEX_WAKE_BITSET`、`FUTEX_REQUEUE` 携带 `FUTEX_CLOCK_REALTIME` 应失败。

2. 地址和对齐校验测试
   - `FUTEX_WAKE(NULL, ...)` 返回 Linux 兼容错误。
   - 未对齐 `uaddr` 返回 `EINVAL` 或 `EFAULT`，按 Linux man page 和实际 syscall 约定确定预期。
   - `FUTEX_REQUEUE`/`FUTEX_CMP_REQUEUE` 的 NULL、未对齐、不可访问 `uaddr2` 返回 Linux 兼容错误。

3. count 参数校验测试
   - `FUTEX_WAKE` 传负数 count 返回 `EINVAL`。
   - `FUTEX_REQUEUE`/`FUTEX_CMP_REQUEUE` 的负数 `val`/`val2` 返回 `EINVAL`。

4. private/shared key 语义测试
   - `MAP_SHARED` 跨 fork 且不带 `FUTEX_PRIVATE_FLAG` 时，child wake 能唤醒 parent。
   - 同样 shared mapping 跨 fork 但带 `FUTEX_PRIVATE_FLAG` 时，不应跨进程唤醒，parent 应 timeout。
   - 保留现有 shared futex count 测试，避免回归。

5. bitset 语义测试
   - 保留已有相交/不相交 bitset 测试。
   - 补 `FUTEX_WAKE_BITSET | FUTEX_CLOCK_REALTIME` 非法组合。
   - 补 `FUTEX_WAIT_BITSET | FUTEX_CLOCK_REALTIME` 使用 realtime absolute timeout 的 accepted path。

6. `FUTEX_CMP_REQUEUE` 测试
   - compare mismatch 返回 `EAGAIN`。
   - compare match 时先 wake source 上最多 `val` 个 waiter，再 requeue 最多 `val2` 个 waiter 到 target。
   - target futex 的后续 wake 能唤醒被 requeue 的 source waiter。
   - 测试应能区分“被直接 wake”和“被 requeue 后由 target wake”。

7. robust-list ABI 测试
   - `get_robust_list` 的 `size` 输出指针非法返回 `EFAULT`。
   - 同进程其他 pthread 的 tid 可查询到其 robust head。
   - 保留已有 `set_robust_list` size 校验和 nonexistent tid 测试。

8. robust owner-death 测试
   - `list_op_pending` 指向节点但链表为空时，owner exit 仍设置 `FUTEX_OWNER_DIED` 并 wake waiter。
   - pending 节点同时在链表中时，最终语义正确即可，不要求暴露处理次数。
   - futex word TID 不等于退出线程 TID 时，不应设置 `FUTEX_OWNER_DIED`，waiter 应 timeout。
   - 坏链表或循环链表不应导致测试永久挂死；如用户态无法稳定断言内核 warning，则只测试 syscall/线程退出可恢复行为。

### Test Agent 交付物

- 测试代码 diff。
- 简短说明每个新增测试对应的 Linux 语义。
- 说明是否新增 helper、同步机制、timeout 策略。
- 不评价 StarryOS 当前实现是否正确，除非主 agent 明确要求你运行并记录结果。

## Subagent 2：Implementation Agent 任务

### 角色与范围

你是内核实现 agent。你根据 Linux 语义和本文档修复 StarryOS 本轮范围内的 futex/robust-list 基础行为。你可以与 test agent 并行工作，不等待其测试完成；但你不能修改测试预期。

写入范围：

- `os/StarryOS/kernel/src/syscall/sync/futex.rs`
- `os/StarryOS/kernel/src/task/futex.rs`
- 必要时：
  - `os/StarryOS/kernel/src/task/ops.rs`
  - `os/StarryOS/kernel/src/task/mod.rs`

范围边界与扩展兼容要求：

- 本轮不实现 PI futex，但接口和结构不要阻碍后续加入 PI owner/priority 语义。
- 本轮不实现 `FUTEX_WAKE_OP`，但 op 解析不要写成难以扩展的新大分支。
- 本轮不新增 ptrace 权限框架，但 `get_robust_list` 的实现不要让后续权限检查难以插入。
- 本轮不修改 `execve` 生命周期，但 robust-list cleanup 逻辑应尽量保持可复用。
- 不用 clippy allow 掩盖问题。

### 详细任务清单

1. futex op 和 flag 解析
   - 集中解析 command、`FUTEX_PRIVATE_FLAG`、`FUTEX_CLOCK_REALTIME`。
   - 明确支持 command：`WAIT`、`WAKE`、`WAIT_BITSET`、`WAKE_BITSET`、`REQUEUE`、`CMP_REQUEUE`。
   - 对本轮未支持的 command 返回 `ENOSYS`。
   - 对 command 支持但 flag 组合非法的情况返回 `EINVAL`。
   - 保持解析代码可扩展，便于后续单独加入 `FUTEX_WAKE_OP` 或 PI futex，而不需要重写已完成的基础语义。

2. `FUTEX_PRIVATE_FLAG` 与 futex key
   - 让 `FUTEX_PRIVATE_FLAG` 强制使用当前进程 private futex table。
   - 未设置 `FUTEX_PRIVATE_FLAG` 时，保留当前按 VMA backend 识别 shared mapping 的逻辑。
   - 调整 `FutexKey::new_current` 等接口，使 private/shared 选择显式可见。
   - robust-list teardown 如暂时无法知道 private flag，保留当前 best-effort 逻辑，并用注释说明限制。

3. 用户指针和参数校验
   - `WAIT`/`CMP_REQUEUE` 继续通过 `vm_read()` 做用户态 compare read。
   - `WAKE` 即使不读取 futex word，也要校验 `uaddr` 是合法 futex word 地址。
   - `REQUEUE`/`CMP_REQUEUE` 校验 `uaddr2`。
   - 对 Linux `int` 语义的 count 参数拒绝负值。
   - 将 `REQUEUE` 中通过 `timeout` 参数传入的 `val2` 转换为清晰命名，避免混淆。

4. WAIT/WAKE/BITSET 语义整理
   - 保持现有 `WAIT` 入队前和队列锁内二次检查用户值的设计。
   - 保持 `WAIT_BITSET`/`WAKE_BITSET` 的 bitset 相交语义。
   - `WAIT_BITSET`/`WAKE_BITSET` 的 `val3 == 0` 继续返回 `EINVAL`。
   - 不把当前普通 `WaitQueue` 抽象成未来 PI futex 必须复用的唯一模型。

5. `REQUEUE`/`CMP_REQUEUE` 语义加强
   - `CMP_REQUEUE` mismatch 返回 `EAGAIN`。
   - compare match 时按 Linux 语义 wake source 上最多 `val` 个，再 requeue 最多 `val2` 个到 target。
   - 尽量保证 compare 与后续 wake/requeue 在 source queue 上序列化，降低竞态。
   - 若需要在 `WaitQueue` 增加组合操作，保持 source/target 双锁顺序稳定，避免死锁。

6. robust-list 普通 futex owner-death
   - 保持 `exit_robust_list()` 的总体结构。
   - 确认 `list_op_pending` 单独处理语义。
   - pending 节点在链表中时不应造成错误的重复副作用。
   - futex word owner TID 不匹配当前退出线程时不置 `FUTEX_OWNER_DIED`。
   - 退出线程只 wake 一个 waiter。
   - 坏链表/limit 不导致内核死循环。

### Implementation Agent 交付物

- 内核实现 diff。
- 简短说明每个语义修复点。
- 明确列出后续扩展项，以及本轮为这些扩展保留的接口/结构空间。
- 若某条 Linux 语义因当前架构限制无法完整实现，必须说明限制和风险。

## Subagent 3：Review Agent 任务

### 角色与范围

你是只读审查 agent。测试和实现都完成后再启动。你的目标是找问题，不是补代码。

### 审查清单

1. 范围与扩展兼容审查
   - 是否把 PI futex、`FUTEX_WAKE_OP`、ptrace 权限、execve robust cleanup 混入本轮实现，或破坏这些后续扩展的接入空间。
   - 是否修改了无关模块。

2. 测试审查
   - 测试是否只按 Linux 语义定义预期。
   - 是否存在为了当前实现定制的弱断言。
   - 是否所有阻塞路径都有 timeout。
   - 并发/跨进程同步是否可靠，是否有明显 flaky 风险。

3. 实现审查
   - op/flag 错误码是否符合 Linux 语义。
   - private/shared key 是否会破坏现有 shared futex。
   - `FUTEX_CLOCK_REALTIME` 是否只在允许场景生效。
   - `REQUEUE/CMP_REQUEUE` 是否存在明显 lost wake 或死锁风险。
   - robust-list owner-death 是否正确处理 pending、owner mismatch、limit。

4. 验证审查
   - 是否运行了必要容器内命令。
   - 如果验证失败，失败是否属于本轮修改。
   - 是否给出足够日志位置或失败摘要。

### Review Agent 交付物

- 按严重程度排序的问题列表。
- 每个问题给出文件/行号或测试名。
- 给出是否建议合入本轮结果的结论。

## 主 Agent 整合与验证

### 整合顺序

1. 并行启动 test agent 和 implementation agent。
2. 收到 test agent 结果后，检查是否遵守写入范围、是否面向 Linux 语义。
3. 收到 implementation agent 结果后，检查是否遵守范围与扩展兼容要求、是否改了测试预期。
4. 合并两边变更，解决冲突时优先保留测试语义，除非测试违反 Linux。
5. 启动 review agent 做只读审查。
6. 根据 review 反馈给implementation agent做最小必要修正。
7. 运行验证命令。

### 推荐验证命令

基础格式和 clippy：

```bash
docker compose run --rm tgoskits cargo fmt
docker compose run --rm tgoskits cargo xtask clippy --package starry-kernel
```

主 syscall 测试：

```bash
docker compose run --rm tgoskits cargo xtask starry test qemu --arch x86_64 -g normal -c syscall
```

futex race 回归：

```bash
docker compose run --rm tgoskits cargo xtask starry test qemu --arch x86_64 -g normal -c test-futex-race
```

如 xtask 参数名在当前环境中不同，使用等价的 `--test-group normal --test-case ...`。

不用补多架构 syscall

## 接受标准

- 测试 agent 的新增测试覆盖本轮所有 in-scope 语义，不依赖 StarryOS 当前实现细节。
- 实现 agent 的修改覆盖本轮范围内的 futex 和普通 robust-list owner-death 基础完善，并保留后续扩展空间。
- `test-futex-robust-list` 通过。
- `bug-futex-wait-wake` 不回归。
- `test-futex-race` 不回归。
- `cargo fmt` 通过。
- `cargo xtask clippy --package starry-kernel` 通过；若失败，必须证明失败是已有无关问题。
- 未把 PI futex、`FUTEX_WAKE_OP`、ptrace 权限框架、execve robust cleanup 混入本轮实现；相关接口没有被写死到难以扩展。
- 最终总结必须列出：完成项、未完成暂缓项、验证命令和结果、剩余风险。
